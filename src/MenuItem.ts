class MenuItem {
  quantity: number;
  product: string;
  price: number;

  constructor(quantity: number, product: string, price: number) {
    this.quantity = quantity;
    this.product = product;
    this.price = price;
  }
}